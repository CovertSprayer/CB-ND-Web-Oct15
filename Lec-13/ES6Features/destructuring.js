const car = {
    color:'White',
    wheels:4,
    topSpeed: '300kmph'
}

let {color:carColor, wheels} = car;

// const carColor = car.color;
// const wheels = car.wheels;
console.log(wheels, carColor);